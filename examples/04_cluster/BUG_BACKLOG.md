# Bug Backlog

## Open

- [x] `todo add` with multi-word title requires quoting — should join all remaining args
- [ ] No validation that status filter in `todo list` is a valid status value
- [ ] IDs are not stable after delete — deleted IDs can be reused on next add

## Resolved

- [x] `todo add` with multi-word title requires quoting — should join all remaining args
